# ZIP source fidelity

`Scholars Full new site.zip` is the visual and interaction source of truth for this project.

Production compatibility changes must not redesign, shorten, restyle, recolor, retype, or restructure the frontend. The only allowed deviations are changes strictly required by the hosting/runtime environment, and those changes should preserve the rendered result and user interaction.
