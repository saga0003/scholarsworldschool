import Image from "next/image";import Link from "next/link";
export function PageHero({image,eyebrow,title,subtitle,crumbs=[]}:{image:string;eyebrow:string;title:string;subtitle?:string;crumbs?:{label:string;href?:string}[]}){return <section className="relative min-h-[52vh] overflow-hidden"><Image src={image} alt={title} fill className="object-cover" priority/><div className="absolute inset-0 bg-navy-deep/75"/><div className="relative mx-auto flex min-h-[52vh] max-w-7xl items-end px-5 pb-14 pt-24 text-white"><div><div className="mb-5 flex gap-2 text-xs text-white/55">{crumbs.map((c,i)=><span key={c.label}>{c.href?<Link href={c.href}>{c.label}</Link>:c.label}{i<crumbs.length-1&&" / "}</span>)}</div><span className="text-sm font-bold uppercase tracking-[.2em] text-gold">{eyebrow}</span><h1 className="mt-3 max-w-4xl font-display text-5xl font-bold sm:text-6xl">{title}</h1>{subtitle&&<p className="mt-4 max-w-2xl text-lg text-white/75">{subtitle}</p>}</div></div></section>}
