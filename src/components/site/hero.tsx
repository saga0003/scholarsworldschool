"use client";
import Image from "next/image";import Link from "next/link";import { ArrowRight } from "lucide-react";import { HERO_SLIDES } from "@/lib/site-data";
export function Hero(){const s=HERO_SLIDES[0];return <section className="relative min-h-[76vh] overflow-hidden"><Image src={s.image} alt="Scholars' School campus" fill priority className="object-cover"/><div className="absolute inset-0 hero-gradient"/><div className="relative mx-auto flex min-h-[76vh] max-w-7xl items-center px-5 py-20"><div className="max-w-3xl text-white"><span className="rounded-full border border-gold/40 bg-gold/10 px-4 py-2 text-xs font-bold uppercase tracking-[.2em] text-gold">{s.eyebrow}</span><h1 className="mt-7 font-display text-5xl font-bold leading-[1.02] sm:text-7xl">{s.pre} <span className="italic text-gold">{s.highlight}</span> {s.post}</h1><p className="mt-6 max-w-2xl text-lg leading-relaxed text-white/80">{s.subtitle}</p><div className="mt-8 flex flex-wrap gap-4"><Link href={s.primaryCta.href} className="inline-flex items-center gap-2 rounded-full bg-gold px-7 py-4 font-bold text-navy">{s.primaryCta.label}<ArrowRight className="h-4 w-4"/></Link><Link href={s.secondaryCta.href} className="rounded-full border border-white/30 px-7 py-4 font-bold text-white">{s.secondaryCta.label}</Link></div></div></div></section>}
