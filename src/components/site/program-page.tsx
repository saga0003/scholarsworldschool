import Image from "next/image";import Link from "next/link";import { Navbar } from "./navbar";import { Footer } from "./footer";import { MobileCtaBar } from "./mobile-cta-bar";import { PageHero } from "./page-hero";import { GalleryGrid } from "./gallery-grid";import { CtaBand } from "./cta-band";
export function ProgramPage({program}:{program:any}){return <div><Navbar/><PageHero image={program.images[0].src} eyebrow={program.kicker} title={program.label} subtitle={program.headline} crumbs={[{label:"Home",href:"/"},{label:"Academics",href:"/academics"},{label:program.label}]}/><main><section className="bg-white py-20"><div className="mx-auto grid max-w-7xl gap-12 px-5 lg:grid-cols-2"><div><h2 className="font-display text-4xl font-bold text-navy">{program.headline}</h2><p className="mt-5 text-lg leading-relaxed text-muted-foreground">{program.intro}</p><Link href="/apply" className="mt-7 inline-block rounded-full bg-gold px-6 py-3 font-bold text-navy">Apply for admission</Link></div><div className="relative aspect-[4/3] overflow-hidden rounded-3xl"><Image src={program.collageImage} alt={program.label} fill className="object-cover"/></div></div></section><section className="bg-cream py-20"><div className="mx-auto max-w-7xl px-5"><GalleryGrid images={program.images}/></div></section><CtaBand/></main><Footer/><MobileCtaBar/></div>}
